name: Build CI Docker Images

# Rebuilds the pre-baked builder images and pushes them to the local Gitea
# container registry (172.0.0.29:3000).
#
# WHEN TO RUN:
#   - Automatically: whenever a Dockerfile under .docker/ changes on master.
#   - Manually: via workflow_dispatch (e.g. first-time setup, forced rebuild).
#
# ONE-TIME SERVER PREREQUISITE (run once on 172.0.0.29 before first use):
#   echo '{"insecure-registries":["172.0.0.29:3000"]}' \
#     | sudo tee /etc/docker/daemon.json
#   sudo systemctl restart docker
#
# Images produced:
#   172.0.0.29:3000/sarman/tftsr-linux-amd64:rust1.89-node22
#   172.0.0.29:3000/sarman/tftsr-windows-cross:rust1.89-node22
#   172.0.0.29:3000/sarman/tftsr-linux-arm64:rust1.89-node22

on:
  push:
    branches:
      - master
    paths:
      - '.docker/**'
  workflow_dispatch:

concurrency:
  group: build-ci-images
  cancel-in-progress: false

env:
  REGISTRY: 172.0.0.29:3000
  REGISTRY_USER: sarman

jobs:
  linux-amd64:
    runs-on: linux-amd64
    container:
      image: alpine:latest
    steps:
      - name: Checkout
        run: |
          apk add --no-cache git docker-cli
          git init
          git remote add origin http://172.0.0.29:3000/sarman/tftsr-devops_investigation.git
          git fetch --depth=1 origin "$GITHUB_SHA"
          git checkout FETCH_HEAD
      - name: Build linux-amd64 builder
        env:
          RELEASE_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        run: |
          echo "$RELEASE_TOKEN" | docker login $REGISTRY -u $REGISTRY_USER --password-stdin
          docker build \
            -t $REGISTRY/$REGISTRY_USER/tftsr-linux-amd64:rust1.89-node22 \
            -f .docker/Dockerfile.linux-amd64 .
      - name: Validate linux-amd64 image
        run: |
          docker run --rm \
            $REGISTRY/$REGISTRY_USER/tftsr-linux-amd64:rust1.89-node22 \
            sh -c "
              set -e
              make --version > /dev/null && echo '✓ make available'
              pkg-config --libs libsodium > /dev/null && echo '✓ libsodium pkg-config OK'
              test -f /usr/lib/x86_64-linux-gnu/libsodium.a && echo '✓ static libsodium present'
              test -n \"\$SODIUM_LIB_DIR\" && echo \"✓ SODIUM_LIB_DIR=\$SODIUM_LIB_DIR\"
            "
      - name: Push linux-amd64 builder
        env:
          RELEASE_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        run: |
          echo "$RELEASE_TOKEN" | docker login $REGISTRY -u $REGISTRY_USER --password-stdin
          docker push $REGISTRY/$REGISTRY_USER/tftsr-linux-amd64:rust1.89-node22
          echo "✓ Pushed $REGISTRY/$REGISTRY_USER/tftsr-linux-amd64:rust1.89-node22"

  windows-cross:
    runs-on: linux-amd64
    container:
      image: alpine:latest
    steps:
      - name: Checkout
        run: |
          apk add --no-cache git docker-cli
          git init
          git remote add origin http://172.0.0.29:3000/sarman/tftsr-devops_investigation.git
          git fetch --depth=1 origin "$GITHUB_SHA"
          git checkout FETCH_HEAD
      - name: Build and push windows-cross builder
        env:
          RELEASE_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        run: |
          echo "$RELEASE_TOKEN" | docker login $REGISTRY -u $REGISTRY_USER --password-stdin
          docker build \
            -t $REGISTRY/$REGISTRY_USER/tftsr-windows-cross:rust1.89-node22 \
            -f .docker/Dockerfile.windows-cross .
          docker push $REGISTRY/$REGISTRY_USER/tftsr-windows-cross:rust1.89-node22
          echo "✓ Pushed $REGISTRY/$REGISTRY_USER/tftsr-windows-cross:rust1.89-node22"

  linux-arm64:
    runs-on: linux-amd64
    container:
      image: alpine:latest
    steps:
      - name: Checkout
        run: |
          apk add --no-cache git docker-cli
          git init
          git remote add origin http://172.0.0.29:3000/sarman/tftsr-devops_investigation.git
          git fetch --depth=1 origin "$GITHUB_SHA"
          git checkout FETCH_HEAD
      - name: Build and push linux-arm64 builder
        env:
          RELEASE_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        run: |
          echo "$RELEASE_TOKEN" | docker login $REGISTRY -u $REGISTRY_USER --password-stdin
          docker build \
            -t $REGISTRY/$REGISTRY_USER/tftsr-linux-arm64:rust1.89-node22 \
            -f .docker/Dockerfile.linux-arm64 .
          docker push $REGISTRY/$REGISTRY_USER/tftsr-linux-arm64:rust1.89-node22
          echo "✓ Pushed $REGISTRY/$REGISTRY_USER/tftsr-linux-arm64:rust1.89-node22"
